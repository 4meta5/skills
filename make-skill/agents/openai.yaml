interface:
  display_name: "Make Skill"
  short_description: "Create and validate skills with canonical taxonomy"
  default_prompt: "Use $make-skill to create or update this skill using the canonical taxonomy and local hooks validation flow."
